version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd red-lantern
        - npm install
    build:
      commands: 
        - cd red-lantern
        - npm install
        - npm run build
  artifacts:
    baseDirectory: red-lantern/build
    files:
      - '**/*'

backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
  artifacts:
    baseDirectory: amplify/backend
    files:
      - '**/*'
  cache:
    paths: 
      - node_modules/**/*